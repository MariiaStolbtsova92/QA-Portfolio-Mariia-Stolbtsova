name: Playwright Registration Tests

on:
  push:
    branches: [master]
    paths:
      - 'Playwright-Registration-Tests/**'
  pull_request:
    branches: [master]
    paths:
      - 'Playwright-Registration-Tests/**'

jobs:
  test:
    timeout-minutes: 30
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: Playwright-Registration-Tests

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Set up Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 20

      - name: Install dependencies
        run: npm ci

      - name: Install Playwright browsers
        run: npx playwright install --with-deps

      - name: Run Playwright tests
        run: npx playwright test

      - name: Upload HTML report
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: playwright-report
          path: Playwright-Registration-Tests/playwright-report/
          retention-days: 7